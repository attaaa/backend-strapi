'use strict';

/**
 * web-setting-contact service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::web-setting-contact.web-setting-contact');
