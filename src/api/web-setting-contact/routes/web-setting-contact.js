'use strict';

/**
 * web-setting-contact router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::web-setting-contact.web-setting-contact');
